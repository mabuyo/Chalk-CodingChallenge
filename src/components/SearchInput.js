import React, { Component } from "react";

class SearchInput extends Component {
  render() {
    return <p style={styles.search}>Search input goes here</p>;
  }
}

const styles = {
  search: {
    color: "white"
  }
};

export default SearchInput;
